HOME = .

openssl_conf = openssl_init

[openssl_init]
providers = provider_sect

[provider_sect]
default = default_sect
parsec = parsec_sect

[parsec_sect]
module = /tmp/parsec-openssl-provider/target/debug/libparsec_openssl_provider_shared.so
activate = 1

[default_sect]
activate = 1
